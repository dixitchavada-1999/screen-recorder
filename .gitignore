# Dependencies
node_modules/

# Build output
out/
dist/
release/

# Generated application icon (rebuilt by `npm run icon`)
build/icon.png

# Local recordings and custom FFmpeg binaries
recordings/*
!recordings/README.md
ffmpeg/*
!ffmpeg/README.md

# Fetched by scripts/fetch-whisper.mjs, not committed — see whisper/README.md
whisper/*
!whisper/README.md

# Logs and caches
*.log
.vite/
.eslintcache

# Editor and OS noise
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Environment overrides
.env
.env.local

# Local MCP client config — carries this machine's live bearer token
.mcp.json

# The release key. Writes to every table and bucket — never commit it.
.env.release

# Supabase CLI working state — machine-local, not part of the schema
supabase/.temp/

# Incremental TypeScript build state. Regenerated by `npm run typecheck`, and
# only ever a source of conflicts when it is tracked.
*.tsbuildinfo

# A bare clone that landed here by accident
call-recording-app/
