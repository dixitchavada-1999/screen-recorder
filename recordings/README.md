# recordings/

A convenience output folder for development.

By default the application saves to your operating system's **Videos** directory
(`C:\Users\<you>\Videos` on Windows, `~/Videos` on Ubuntu). If you would rather
keep test recordings inside the project while working on it, point the app here:

**Settings → Storage → Output folder → Browse** → select this folder.

Everything in here except this file is git-ignored.
